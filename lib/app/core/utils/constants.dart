class AppConstants {
  // Animation durations
  static const splashDuration = Duration(seconds: 3);
  static const animationDuration = Duration(milliseconds: 500);
  
  // Storage keys
  static const seenOnboardingKey = 'seen_onboarding';
}